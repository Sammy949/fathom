node_modules/
.next/
out/
dist/
build/
.vercel/

# Secrets — never commit
.env
.env.local
.env.*.local
*.key
keystore/

# Logs / caches
*.log
npm-debug.log*
.turbo/
.eslintcache
coverage/
.tsbuildinfo
*.tsbuildinfo

# OS
.DS_Store
Thumbs.db

# Local registry override (npmmirror is slow here) — environment-specific
.npmrc

# Next.js app (its own .gitignore covers node_modules/.next; lockfile is committed)
apps/web/.next/
apps/web/node_modules/
